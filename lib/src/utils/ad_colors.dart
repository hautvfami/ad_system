import 'package:flutter/material.dart';

/// Colors used specifically for the ad system to maintain consistent styling.
class AdColors {
  /// Overlay color for glass effects
  static const Color overlay = Color(0xFFEFEFEF);

  /// Border color for ad containers
  static const Color borderColor = Color(0x1A000000);

  /// Primary accent color for ad highlights
  static const Color accent = Color(0xFF2196F3);

  /// Color for ad labels and tags
  static const Color adLabel = Color(0xFF757575);

  /// Color for close buttons on ads
  static const Color closeButton = Colors.black54;
}
